# Contributing

Testing Locally:

```shell
asdf plugin test swagger-codegen https://github.com/gustavorps/asdf-swagger-codegen.git 'swagger-codegen version'
```

Tests are automatically run in GitHub Actions on push and PR.
